import { GroupidToNamePipe } from './groupid-to-name.pipe';

describe('GroupidToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupidToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
