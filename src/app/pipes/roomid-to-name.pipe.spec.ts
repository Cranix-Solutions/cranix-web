import { RoomidToNamePipe } from './roomid-to-name.pipe';

describe('RoomidToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomidToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
