import { InstituteidToNamePipe } from './instituteid-to-name.pipe';

describe('InstituteidToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new InstituteidToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
